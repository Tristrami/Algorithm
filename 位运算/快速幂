/*
求a的b次方对 p取模的值。

输入格式: 三个整数 a,b,p,在同一行用空格隔开
输出格式: 输出一个整数，表示a^b mod p的值
数据范围: 0≤a,b≤109, 1≤p≤109

输入样例：
3 2 7
输出样例：
2
*/

#include <iostream>

using namespace std;
typedef long long ll;

ll quick_pow(ll a, ll b, ll p)
{
    ll ans = 1 % p;
    while (b)
    {
        if (b & 1) ans = ans * a % p;
        b >>= 1;
        a = a * a % p;
    }
    return ans;
}

int main()
{
    ll a, b, p;
    cin >> a >> b >> p;
    cout << quick_pow(a, b, p);
    return 0;
}
